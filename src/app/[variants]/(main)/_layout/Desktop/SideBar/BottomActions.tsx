import { memo } from 'react';
import { Flexbox } from 'react-layout-kit';

const BottomActions = memo(() => {
  return (
    <Flexbox gap={8}>
      {/* GitHub and user manual icons removed as per rebranding requirements */}
    </Flexbox>
  );
});

export default BottomActions;
